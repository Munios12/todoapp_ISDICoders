// import { render, screen } from '@testing-library/react';
// import { Layout } from './Layout';

// test('renders learn react link', () => {
//   render(<Layout />);
//   const layoutTitle = screen.getByText(/Learning React/i);
//   expect(layoutTitle).toBeInTheDocument();
// });
